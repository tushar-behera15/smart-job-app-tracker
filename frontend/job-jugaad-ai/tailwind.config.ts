import { Config } from 'tailwindcss';

const config: Config = {
    content: ["./src/**/*.{html,ts}"],
    darkMode: 'class',
    theme: {
        extend: {},
    },
    plugins: [],
};

export default config;
